server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ai_teacher_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: as876857
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  main:
    allow-circular-references: true

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.ai_teacher_system_back.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: ai_teacher_system_secret_key_2023abcdefghijklmnopqrstuvwxyz0123456789
  expiration: 86400000  # 24小时

logging:
  level:
    com.example.ai_teacher_system_back: debug